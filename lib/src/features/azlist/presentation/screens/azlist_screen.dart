import 'package:flutter/material.dart';

class AzlistScreen extends StatelessWidget {
  const AzlistScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('A-Z List'),
      ),
    );
  }
}
